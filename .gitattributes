# The Anytype API client is fully generated from api/openapi.yaml via
# `go generate ./...`. A textual 3-way merge of it is never the right tool
# (regeneration always produces the correct result), so keep our side on merge
# and regenerate after rebasing rather than resolving conflicts by hand.
internal/api/anytype.gen.go merge=ours linguist-generated=true
